const prompt = require("prompt-sync")();
let Age = Number(prompt("entrer votre age : "))

if (Age>18) {
            console.log("Éligibl de voter ")
           }
else {
     console.log("non Éligibl ") 
     }
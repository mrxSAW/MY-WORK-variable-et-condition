const prompt = require("prompt-sync")();
let A = Number(prompt("entrer le 1er nombre : "))
let B = Number(prompt("entrer le 2eme nombre: "))


if (A>B){
         console.log(A +" et supirier a " + B )
        }
else {
     console.log(B + " est supirieur a " + A)
     }



     